services:
  - type: web
    name: vuln-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    disk:
      name: vuln-data
      mountPath: /data
      sizeGB: 1
 
